site_name: DNAStream Documentation
repo_url: https://github.mdanderson.org/llweber/DNAStream
theme:
  name: material

nav:
  - Home: index.md
  - API Reference:
      - DNAStream: api/dnastream.md

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          inventories: []  # Fix warning by replacing 'import' with 'inventories'
          paths: ["src"]
          options:
            docstring_style: numpy
            show_source: false